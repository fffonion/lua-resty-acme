# resty.acme.crypto.openssl

LuaJIT FFI based OpenSSL backend. API-camptible with [luaossl](https://github.com/wahern/luaossl/).

Currently only part of API that is needed by `resty.acme` library is implemented.